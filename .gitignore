# Build output
/dist/
/bin/

# `go build ./cmd/<name>` drops the binary in the working directory, so a plain
# build at the repository root leaves one here, untracked and easy to commit by
# accident.
/release
/workflow
/agent
/llm
/loop

# Test and coverage artifacts
*.out
*.test

# Editor and OS noise
.DS_Store
.idea/
.vscode/
